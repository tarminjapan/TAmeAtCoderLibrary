using System;
using System.Collections;
using System.Collections.Generic;
using System.ComponentModel;
using System.IO;
using System.Linq;
using System.Net;
using System.Text;

// // -------------------- DEBUG --------------------
// using var st = new StreamReader(@"AtCoderCSharp\Inputs\random_09.txt", System.Text.Encoding.GetEncoding("UTF-8"));
// var inputs = st.ReadLine().Split().Select(int.Parse).ToArray();
// var iN = inputs[0];
// var iQ = inputs[1];
// var iA = st.ReadLine().Split().Select(int.Parse).ToArray();
// var iLRV = Enumerable.Range(0, iQ).Select(_ => st.ReadLine().Split().Select(int.Parse).ToArray()).ToArray();
// // -----------------------------------------------